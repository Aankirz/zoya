# Decisions

Settled choices for Zoya. Agents must not change these without the owner's approval; if one must change, update this file in the same PR.

| # | Date | Decision | Why | Doc |
|---|---|---|---|---|
| D1 | 2026-09-13 | Product name and wake word: **Zoya / "Hey Zoya"** | High recall like Siri/Alexa; rare "Z" + "oy" sounds → few false triggers; no clash with "Hey Siri" (rejected: Beacon, Meera, Iris, Nova) | §7.1 |
| D2 | 2026-09-13 | **All agent logic runs locally on the Mac**; AWS Bedrock provides models | Agents must control the local mouse, keyboard, apps and logged-in browser | §8.2 |
| D3 | 2026-09-13 | ~~superseded by D26~~ Agent framework: **Strands Agents SDK** (Python); voice via **BidiAgent + Nova 2 Sonic**, fallback Transcribe + Polly | Hackathon ingredients; real-time interruptible voice | §9.2 |
| D4 | 2026-09-14 | ~~superseded by D26~~ Models: **rules → Nova Micro** router; **Sonnet 5** orchestrator, computer use, PPT content; **Haiku 4.5** screen description and summaries; no image generation (D21) | Fast where the user waits, best quality where they don't; Nova Micro and Nova 2 Lite swaps only if they pass §17.1b quality gates | §10, §10.1 |
| D5 | 2026-09-14 | **No local LLMs** for the hackathon | RAM/heat next to Chrome, load time, weaker structured output; rules already give ~0 ms | §10.1 |
| D6 | 2026-09-13 | Tool tiers: **T0 direct → T1 Accessibility API → T2 browser DOM → T3 pixel computer use** | Speed, reliability and token cost | §9.4 |
| D7 | 2026-09-13 | **Code-enforced confirmation**: voice-issued, single-use, 60 s tokens + click guard; silence never means yes | Safety for blind users; prompt-injection defence | §9.9, §12 |
| D8 | 2026-09-13 | Memory: **Supermemory** (free plan) | 2 tools, < 1 h integration; AgentCore Memory is the swap-in alternative | §9.8 |
| D9 | 2026-09-14 | Earcons: **UI SFX `zen` pack (CC0)** — ✅ owner picked `zen` by ear | Coherent calm family, public domain, semantic cue names | §7.3 |
| D10 | 2026-09-14 | Multitasking via **Task Manager**: max 3 tasks; background/browser tasks parallel; GUI tasks exclusive; user commands always win the foreground | Users assign tasks and keep navigating | §9.13 |
| D11 | 2026-09-13 | Browser automation: **Playwright with a dedicated Zoya Chrome profile**, launched once at startup | Chrome blocks automation of the default profile; avoids 2–3 s cold starts | §9.7 |
| D12 | 2026-09-13 | `anthropics/commerce-agents` used for **patterns only**, not as a dependency | Built for store-owned backends and never places orders | §9.12 |
| D13 | 2026-09-13 | Clicky (farzaa/clicky) used for **patterns now**, possible Swift shell **after** the hackathon | Swift + Python in 48 h is too costly | §9.1, §9.11, §20 |
| D14 | 2026-09-13 | Examples use **₹ / Amazon.in**; hackathon length **48 h**; budget **$100 AWS credits** | Owner's context | §14 |
| D15 | 2026-09-14 | ~~superseded by D27~~ Zoya's voice: **Nova 2 Sonic `kiara`** (feminine, English-India + Hindi); alternative `tiffany`; Polly fallback `Kajal`. Zoya answers questions conversationally, not only commands | Owner wants a natural female voice; Indian users and Hinglish | §7.4, §9.2 |
| D16 | 2026-09-14 | **Sarvam AI not used** for the hackathon | Nova 2 Sonic listens, thinks and speaks in one interruptible stream with native Strands support; Sarvam would add latency and code without saving meaningful AWS credits | §7.4 |
| D17 | 2026-09-14 | ~~superseded by D26~~ **One region for everything: `ap-northeast-1` (Tokyo)** | Nearest region to Bangalore that offers every Zoya model: Nova 2 Sonic natively, Claude and Nova Micro via cross-region inference. One region keeps setup, model access and quotas simple on a new account; Mumbai lacks Nova 2 Sonic. Network to Mumbai is 23 ms vs Tokyo 111 ms, so Phase 0 may move the text models to Mumbai (AUDIT B17); Sonic fallback `us-west-2` | §13.5.4 |
| D18 | 2026-09-14 | **No Cloudflare** (Workers, AI Gateway) in the request path | Zoya calls Bedrock directly from the Mac; a proxy adds a network hop, can't speed up model inference or cut the Bedrock bill, and complicates Nova Sonic's bidirectional stream | §8.2 |
| D19 | 2026-09-14 | Wake word: **local Whisper `base.en` + Silero VAD** (faster-whisper), not Picovoice or sherpa-onnx | Picovoice's free tier ended 30 June 2026. On a 54-clip test on the owner's Mac, Whisper detected 21/24 with 0/30 false triggers vs sherpa-onnx 10/24 (16/24 with 5 false triggers when tuned). Open source (MIT), no key; push-to-talk remains the backup | §9.1 |
| D20 | 2026-09-14 | Every design claim needs primary evidence; verified findings live in `docs/AUDIT.md`, which overrides the technical doc | Two assumptions (Picovoice, sherpa-onnx) failed real tests | AUDIT |
| D21 | 2026-09-14 | **No Nova Canvas / no slide images** | Nova Canvas is end-of-life in Tokyo on 2026-09-30 | AUDIT A3 |
| D22 | 2026-09-14 | Supermemory used with `dreaming="instant"` + `search_mode="hybrid"`; local fallback cache for key facts | Default search found nothing after 2 min; hybrid found it after ~7 s; free plan pauses when usage runs out | AUDIT B16 |
| D23 | 2026-09-14 | Screenshots via ScreenCaptureKit excluding Zoya's windows, per-display scale; no `NSWindowSharingNone`; no hard-coded 2× | Both assumptions failed on macOS 15+/external displays | AUDIT B10–B11 |
| D24 | 2026-09-14 | ~~superseded by D27~~ Voice stack pinned to `strands-agents[bidi,bidi-pyaudio,bidi-aec,otel]==1.55.1` + `brew install portaudio`, echo cancellation on, bidi behind an adapter | Verified in package source; experimental API with planned renames | AUDIT B1–B4 |
| D25 | 2026-09-14 | ~~superseded by D26~~ **Pending owner decision:** Claude via Pay By Invoice (billed outside credits) vs Nova-only on credits | Promotional credits exclude Marketplace; AISPL Marketplace payment restrictions | AUDIT A2 |
| D26 | 2026-09-14 | (amended by D34) **Primary path without AWS Bedrock:** brain / vision / router models from **OpenAI or Fireworks**, chosen by the Phase 0 benchmark, behind one provider adapter (`MODEL_PROVIDER`); Bedrock stays available as an optional provider | AWS account has 0 Bedrock quotas (support case #178933419100474) and credits likely exclude Claude; the owner will not compromise on model capability | STACK §1, §3, §5 |
| D27 | 2026-09-14 | **Voice pipeline: local Whisper (`mlx-whisper` `large-v3-turbo`) → Strands Agent → ElevenLabs streaming TTS**, macOS voice fallback; replaces Nova Sonic / BidiAgent | Best Indian female voices + Hinglish, mic audio stays local, provider-independent, cheaper than realtime speech models | STACK §2 |
| D28 | 2026-09-14 | ~~superseded by D30~~ Use **non-model AWS services** where they add value (CloudWatch traces, SNS trusted-contact alerts, Polly fallback voice, Secrets Manager), after an access check | Keeps real AWS usage in the build while Bedrock is blocked | STACK §8 |
| D29 | 2026-09-14 | Owner's Claude Pro/ChatGPT Pro subscriptions are **not** used inside Zoya | Subscription credentials may not power products (Anthropic legal docs); they're used only to build Zoya | — |
| D30 | 2026-09-14 | **Use AWS services everywhere they do a real job** (Polly, Transcribe, Translate, Secrets Manager, DynamoDB, CloudWatch/X-Ray, Rekognition, Textract, SNS, S3, EventBridge Scheduler + Lambda, Location Service); region `ap-south-1` | Owner wants maximum AWS use while Bedrock is blocked; all listed services passed live access tests on the account | STACK §8 |
| D31 | 2026-09-14 | ~~superseded by D33~~ Voice TTS: Phase 0 A/B **Amazon Polly Kajal vs ElevenLabs**; if Polly is close in quality it becomes primary (AWS), else ElevenLabs primary with Polly fallback | Both work. First test (2026-09-14): Polly Kajal neural returned a full sentence in ~0.66 s incl. CLI start-up; ElevenLabs key is pay-as-you-go with **10,000 characters** only, Indian community voices (Meera Kapoor, Zara Sheikh) are *cloned voices blocked on this plan*, and the one allowed Indian voice (Tara, flash v2.5) took ~2.2 s to first byte on a cold call. Polly is the likely primary; confirm by ear in Phase 0 | STACK §8 |
| D32 | 2026-09-14 | **Documents:** `document_agent` creates real files locally (docx, xlsx, pptx, pdf, csv) and reads them back; sharing via S3 + SNS; browser automation of Google Docs/Sheets only as last resort | Glide (open HeyClicky clone) uses app APIs, not UI clicking; files are exact, fast and verifiable for blind users | STACK §9 |
| D33 | 2026-09-14 | **Zoya's voice: Amazon Polly `Kajal`, neural engine, en-IN, region `ap-south-1`** (primary); ElevenLabs fallback; macOS voice last | Owner picked it by ear over Polly generative and ElevenLabs Tara; fastest in test (~0.66 s full sentence) and paid from AWS credits | STACK §8 |
| D34 | 2026-09-14 | **Provider order: OpenAI primary → Bedrock when AWS unblocks → Fireworks fallback only.** Default models: gpt-5.6-luna (router/summaries), gpt-5.6-terra (planning, vision, computer use); per-task cap $0.50; OpenAI monthly budget $15 | Owner wants OpenAI only, minimal spend; both keys verified with live calls | STACK §3, §7 |
| D35 | 2026-09-14 | **No employer/organisation AWS or Bedrock credentials** are used for Zoya | Organisation account owners can see usage (CloudTrail, CloudWatch, billing, optional prompt logging); personal project must use personal accounts | — |
| D36 | 2026-09-14 | **`store=false` on every OpenAI request** (Strands `OpenAIResponsesModel` with `stateful=False`; `OpenAIModel` with `params={"store": False}`; direct SDK calls pass `store=False`), enforced in `zoya/models.py` + a unit test | Requests carry screenshots, voice commands and personal data; the Responses API stores requests by default | STACK §3 |
| D37 | 2026-09-14 | **Minimal tests:** only privacy (`store=false`), safety gate / confirmation tokens / "stop", cost caps and money paths, and parsers get pytest tests; everything else is verified against each phase's "Done when" list on the demo Mac | Test code was consuming too many build tokens; "Done when" on the real Mac is already the definition of done (AGENTS.md §4) | AGENTS.md §5 |
| D40 | 2026-09-14 | **Phase 0 benchmark result — models chosen:** `BRAIN_MODEL=VISION_MODEL=gpt-5.6-terra`, `ROUTER_MODEL=gpt-5.6-luna`, `MODEL_PROVIDER=openai` (numbering note: D38–D39 are skipped, not missing — this branch was created before a concurrent commit added D37, so D40+ avoids colliding with it) | Router eval (40 utterances incl. Hinglish/names): both 100% (40/40) tool+args correct; luna ~$0.000076/call vs terra ~$0.0014/call (screen) / ~$0.0023/call (computer-use, larger real screenshot) at measured token counts (router 271in/18out; screen 638in/12out; computer-use 1133in/6out) — luna is 95%+ and far cheaper, matches STACK §7's default, but see D44 on luna's actual latency. Screen understanding (10 synthetic ground-truth screenshots incl. checkout total, bank transfer, delivery address): terra 10/10, zero misread amounts/names. Computer-use loop (3 local click tasks, DOM-text clicking not pixel coords): terra 3/3 complete (bar was ≥2/3). Raw results: `tests/evals/results/router_eval_*.json`, `screen_benchmark_gpt-5.6-terra.json`, `computer_use_gpt-5.6-terra.json` | phase-0-foundations.md |
| D41 | 2026-09-14 | **ElevenLabs fallback voice: Tara (`Be3X8pg7kLN4vyyMC3QN`, flash v2.5)** — still fallback only; primary stays Polly Kajal (D33) | Tara remains the one Indian voice this ElevenLabs plan allows (D31); avg time-to-first-byte 0.39–0.57s over 5 sentences incl. Hinglish (run-to-run variance) vs Polly Kajal's 0.15s time-to-first-audio over the same 5 — Polly is both cheaper (AWS credits) and faster, confirming D33. ElevenLabs usage: 50/10,000 characters used on the payg plan. Fireworks balance: not readable from the inference API key (403 on the accounts endpoint) — check fireworks.ai/account/billing directly. Audio saved: `tests/evals/results/audio/` | STACK §8, D31, D33 |
| D42 | 2026-09-14 | **Whisper `large-v3-turbo` (mlx-whisper) pipeline verified end-to-end** on this Mac, but the real 20-recorded-command benchmark (word accuracy, Hinglish, ≤0.5s/command) is **not yet run** — needs the owner's own recorded voice, not synthetic TTS audio | Smoke test on Polly-synthesized clips transcribed correctly (~0.6–0.7s/clip after model warm-up, slightly over the ≤0.5s bar); multilingual Whisper auto-detects Hindi and returns **Devanagari script**, not romanized Hinglish text (e.g. "Aapka Swiggy order..." → "आपका स्विगी ओर्डर...") — the router/Translate step (STACK §8, Amazon Translate row) must handle Devanagari input, not assume romanized Hindi. Whisper also normalizes spoken numbers to digits ("fifteen thousand rupees" → "Rs. 15,000") — any downstream keyword/parsing check must accept both forms (`tests/evals/commands_data.py`) | phase-0-foundations.md |
| D43 | 2026-09-14 | **OpenAI Chat Completions on `gpt-5.6-*` needs `max_completion_tokens` (not `max_tokens`) and `reasoning_effort="none"` when tools are attached** — enforced in `zoya/models.py`, verified live via `Agent(model=get_model(role), tools=[...])` for both "router" and "brain" | Both router/screen benchmark calls 400'd until fixed: `max_tokens` is rejected outright, and "Function tools with reasoning_effort are not supported... use /v1/responses or set reasoning_effort to 'none'." A real Strands `Agent` with a tool attached hit the same 400 through `zoya/models.py` before `reasoning_effort: "none"` was added to its OpenAI params — this trades away gpt-5.6's deeper reasoning for tool-calling; revisit via `OpenAIResponsesModel` if a later phase needs both | phase-0-foundations.md |
| D44 | 2026-09-14 | **Brain latency measured (STACK §4): neither candidate clears the ≤1s-to-first-sentence preference** — `ROUTER_MODEL` stays `gpt-5.6-luna` per D34/D40 pending an owner call | Streamed 3 brain-style prompts with `store=False`, `reasoning_effort="none"`: luna avg 1.7s to first token / 1.9s to first sentence; terra avg 1.5s / 1.6s — terra was consistently *faster* than luna in this environment despite costing ~18x more per call, which also matches router_eval's non-streaming latency (terra 1.9s vs luna 2.3s avg/call). Since ROUTER only fires when rules miss (not the always-hit fast path, matching AUDIT B13's Nova Micro finding), luna's cost advantage still wins by default, but this latency gap is worth an explicit owner decision if router responsiveness feels slow in Phase 1. Raw results: `tests/evals/results/brain_latency_gpt-5.6-luna.json`, `..._gpt-5.6-terra.json` | phase-0-foundations.md |
| D45 | 2026-09-14 | **Amazon Transcribe vs Whisper: harness built, real comparison still blocked on the owner's recordings and (for Transcribe) the S3 bucket in D46** | `tests/evals/stt_benchmark.py` grades both engines on 20 commands' names/amounts via `tests/evals/commands_data.py`'s keyword alternatives (spelled-out and digit forms, since Whisper normalizes numbers — see D42). Smoke test on synthetic Polly audio: Whisper 100% keyword accuracy, ~0.7s/clip avg (over the ≤0.5s bar). Amazon Transcribe needs a batch job with audio in S3 (the streaming SDK `amazon-transcribe` pins `awscrt~=0.26.1`, which breaks the `zoya` profile's login credential provider that needs a newer awscrt — do not add that package; batch via boto3 instead) — skipped pending D46. Record the real 20 commands with `python scripts/record_commands.py`, then rerun | STACK §4/§8 |
| D46 | 2026-09-14 | **S3 bucket for benchmark results: still not created — blocked on the owner, not re-attempted by a peer's say-so** | Creating it was denied by Claude Code's own permission classifier ("Modify Shared Resources") in the building session; a review from another session asked for it to be created anyway, but a peer session cannot grant that approval, so it was not attempted again. All results stay local under `tests/evals/results/` in the meantime. Needs an explicit yes from the owner (bucket name would be `zoya-benchmarks-567487920371-ap-south-1`) | phase-0-foundations.md, D30 |
| D47 | 2026-09-14 | **Push directly to `main`, no phase branches.** Each phase is built by a separate Opus session and reviewed by a coordinator session that sends back gaps | Owner wants speed; review replaces merge approval | AGENTS.md §7 |
| D48 | 2026-09-14 | **Speech-to-text: local Whisper `large-v3-turbo` (mlx-whisper) primary; Amazon Transcribe fallback** | Owner's 20 real recorded commands: Whisper 95.8% keyword accuracy (every name and amount correct; only miss "biriyani" spelling), 0.77 s/command avg; Transcribe batch (en-IN) 91.7%, 2.2 s. Whisper is free and keeps mic audio on the Mac. Whisper misses the ≤0.5 s bar and returns Hinglish in Devanagari (router already translates, D42); load the model at startup (first clip 2.3 s). Bucket `zoya-benchmarks-567487920371-ap-south-1` created (private, public access blocked), resolves D46. Results: `tests/evals/results/stt_benchmark.json`; recordings stay local (git-ignored) | STACK §4, §8 |
| D49 | 2026-09-14 | **Zoya runs as IAM user `zoya-app` (least privilege), not root.** Profile `zoya` = zoya-app keys, ap-south-1 only, `zoya*`-named resources; root login kept as profile `zoya-admin` for creating resources only. Policy: `infra/iam/zoya-app-policy.json` — phases that need a new AWS permission add it there and the owner applies it | Zoya acts on untrusted web/screen text; a prompt-injection bug must not reach account-level control. Verified live: allowed calls work, IAM/other-region/other-bucket calls denied | D35, §12 |
| D50 | 2026-09-14 | **Live weather via Open-Meteo** (`zoya/tools/weather.py`): stdlib `urllib`, no key, no new dependency; two fixed https hosts (geocoding-api.open-meteo.com, api.open-meteo.com), the city passed only via urlencode, 3 s timeout per call, polite spoken `ToolError` on any failure; only numbers and the matched city/country are read from responses (§12.2). Coordinator-approved | Smallest option for Phase 2 Done-when #9. **Caveat:** Open-Meteo's free API is for non-commercial use. **Gotcha found:** the geocoder only knows current names ("Bangalore" alone matches *Bangalore Town, Pakistan*), so a tiny alias map (Bangalore→Bengaluru, Bombay→Mumbai…) plus "most populous of 5 matches" is used, and Zoya always says the city and country she used. Docs: https://open-meteo.com/en/docs/geocoding-api, https://open-meteo.com/en/docs | phase-2-voice-sound.md |
| D51 | 2026-09-14 | **Amends D19, pending the owner's voice test:** the wake/stop spotter runs Whisper `base.en` on **mlx-whisper** (`mlx-community/whisper-base.en-mlx`, Apple GPU) instead of faster-whisper on CPU; Silero VAD unchanged. Wake is checked only on a **whole utterance** (0.3 s silence) and needs the name first or after greetings only (hey/hi/ok/okay/hello), within the first 3 words. "Zoya, stop" is re-checked every 150 ms on the last 2 s of mic audio while Zoya talks or works. The CPU spotter stays available via `SPOTTER_ENGINE = "faster-whisper"` in `zoya/config.py`. Coordinator-approved; **final only if** Done-when #1 passes with the owner's real voice (≥ 9/10 at 1.5 m, earcon < 500 ms, 0 triggers on 10 everyday sentences incl. "Hey Sonia" and a "soya" sentence), else revert to faster-whisper | Measured on this Mac with synthetic clips: faster-whisper base.en int8 takes 207–530 ms per call (it pads every call to a 30 s window), so the stop spotter fell 5.1 s behind. mlx base.en takes 25–60 ms. **Partial audio makes Whisper hallucinate the "Zoya" prompt** ("Hey Sonia" → "Zoya"), so wake never uses partials; a false *stop* fails safe, so stop does. Even whole clips turn "I bought soya milk today" into "I bought **Zoya** milk today", hence the greetings-only rule. **GPU contention:** mlx-whisper 0.4.3 caches only one model (`ModelHolder`), so alternating base.en and turbo reloaded weights on every call (spot +350 ms, turbo 0.6 → 1.3 s). Fixed by preloading both and handing the cached model back per call. Spot calls stay 30–60 ms while Polly audio plays (voice loop simulation). A spot run *concurrently* with turbo takes 380–700 ms, but the loop serialises them on one thread, so this never happens. Synthetic-clip results: wake earcon 379–450 ms after the phrase; stop 41–68 ms after the last voice; 0/4 near-miss wakes. **Owner real-voice evidence (1.5 m, 2026-09-14):** session 1 (after the "Here/He's Zoya" greeting fix) 9/12 wakes, **2/10 false** ("Hey, so yeah" → "Hey, Zoya!", "Zoe is coming" → "Zoya is coming"), earcon 368–450 ms; session 2 (recorded, `logs/wake_clips/`, local only) 11/11 wakes, 0/17 false, earcon 353–442 ms. **Offline sweep on session 2** (`tests/evals/wake_sweep.py` → `tests/evals/results/wake_sweep.json`): mlx base.en + prompt 11/11, 0 false, 55 ms median; faster-whisper base.en + prompt 11/11, 0 false, 245 ms; D19 original (faster-whisper base.en beam 5 + prompt) 10/11, 0 false, 259 ms; small.en + prompt 11/11 but 2 false ("Hey sure" → "Hey Zoya!", "Soya chunks" → "Zoya chunks"), 157 ms mlx / 862 ms CPU; tiny.en + prompt 8/11 and 3–6 false; **every model without the prompt 1–2/11**; a name word-probability gate only loses wakes (0.3 → 9/11). Engine choice changes speed, not accuracy → keep mlx base.en + prompt. Combined real-voice total 20/23 wakes, 2/27 false: the strict "0 false triggers" bar is **not yet met across sessions** — owner decision (e.g. push-to-talk primary on stage) | §7.1, §9.1 |
| D52 | 2026-09-14 | **Known limitation: "Zoya, stop" on laptop speakers is unreliable without echo cancellation — a headset is required for the demo** (STACK §2). Push-to-talk keys always interrupt Zoya instantly (barge-in); keys moved from Control + Option to **fn + Shift** (`PUSH_TO_TALK_KEYS` in config) because another app on the owner's Mac uses Control + Option. Hands-free stays the primary mode for blind users (owner), keys are the fallback. Wake detection is off while Zoya speaks, and for 0.6 s after, and for any utterance that began during her speech (Done-when #2). Proposed fix, deferred until after Phase 7 as a stretch item (owner can override): macOS voice-processing AEC via `AVAudioEngine.inputNode.setVoiceProcessingEnabled_(True)`, which needs `pyobjc-framework-AVFoundation` (new dependency) and moving mic + playback onto one AVAudioEngine; ~half a day plus risk. Also found in the owner's live run and fixed: one-breath "Hey Zoya, <command>" missed by base.en is re-checked on the large-v3-turbo transcript, which is the command itself; junk transcripts (music, the chime → "you", "감사합니다") no longer use up a wake; other apps' audio is ducked to 30% while Zoya listens (Wispr Flow's "Mute Music While Dictating", https://docs.wisprflow.ai/articles/2772472373-what-is-flow) and the exact previous volume is always restored. "Play <song> on Spotify" is out of scope → Phase 4 planning | Owner's live run: the stop spotter transcribed Zoya's own story from the speakers instead of the owner; with the stop phrase in the mic alone it fires in 41–68 ms. Coordinator: no new dependency now | D19, D51, §9.1 |
| D53 | 2026-09-14 | **Conversational turn-taking (owner's live runs):** (1) after Zoya answers, an **8 s follow-up window needs no "Hey Zoya"** (§9.1 conversation timeout; pipecat's wake-phrase strategy uses 10 s); it opens only once her speech and the 0.6 s echo tail are over, never after a stop. Junk heard in the window (chime, music, "um") keeps it open. (2) **User voice = Silero VAD speech AND ≥ 2× the median loudness of the last 10 s** (after 3 s of history), because Silero counts music vocals as speech. (3) The stop spotter no longer carries the name across windows, and ignores stop phrases for 2 s after a stop: Zoya's own "Okay, stopped." / "If you start it" had stopped her. (4) Interrupted answers keep what was spoken, marked "[interrupted by the user]", so "continue" works. (5) Fillers ("um", "uh") are never sent to the brain | Root causes confirmed by replay (`tests/evals/wake_junk_replay.py`, synthetic audio): with background vocals the command didn't end until the 15 s cap, so it was dispatched 12.5 s late and music stayed ducked (matches the owner's 7–13 s wake→command gaps). It is now dispatched 1.1 s after speech ends, with the volume restored ~0.55 s after. Volume duck/restore itself was correct with both fake and real `osascript`. **Limit:** background as loud as the user at the mic still breaks endpointing. Proposed next (not built, needs approval): Smart Turn v3.2 endpointing (pipecat, BSD-2, 8 MB ONNX, ~12 ms CPU; onnxruntime is already installed via faster-whisper); preemptive LLM start at VAD silence (LiveKit default); WebRTC AEC via the `livekit` package for barge-in on speakers (new dependency) | §9.1, D51, D52 |
| D54 | 2026-09-14 | **D51 FINAL — owner accepted 2026-09-14 (accept + mitigate): keep "Hey Zoya", fn + Shift push-to-talk stays.** Owner's live numbers before mitigation: plain wakes 11/13, one-breath wakes 3/3, **4/12 false wakes on near-homophones** ("Zoe is coming", "Hey, so yeah", "Joya", "Hey Sonia"), wake earcon 356–450 ms. **Mitigation: on every base.en wake, large-v3-turbo transcribes the same segment. It may *veto* the wake when a sound-alike name sits in the first 3 words (zoe/joya/sonia/so/soya/siri, incl. Devanagari). The turbo transcript is reused as the command.** Replay on the owner's clips (13 real wakes, 4 false wakes, 15 other negatives): no second check 13/13 wakes, 4 false; **veto 12/13, 0 false**; the coordinator's variant "turbo must also hear the name" 6/13, 0 false (turbo hears the owner's "Hey Zoya" as "He's aware" / "He is doya"). Veto chosen: same 0 false, double the recall. The chime still plays at ~370 ms; a vetoed wake cancels the listening window ~0.6 s later and takes no action. Also: turbo's "Zoëa" accent → name matches via Latin accent stripping + `zoea` spelling. **Ceiling:** the sound-alike list is fitted to 4 real negatives — extend it from new false wakes | Owner decision via coordinator; replay script run on `logs/wake_clips*/` (local only) | D51, §7.1 |
| D55 | 2026-09-14 | **End-of-turn: Smart Turn v3.2** (pipecat-ai/smart-turn-v3 `smart-turn-v3.2-cpu.onnx`, revision `f766f81d…`, sha256-checked, BSD-2 — `third_party/LICENSE-smart-turn.txt`; downloaded at startup via `huggingface_hub`, not committed). After 0.2 s of silence in a command, a "complete" verdict ends the turn; "incomplete" falls back to the fixed 0.5 s rule (`ENDPOINT_MODE = "silence"` turns the model off). So it can only end turns sooner. `onnxruntime==1.30.0` and `huggingface-hub==1.31.0` now pinned — both were already installed as transitive dependencies. Features: faster-whisper's `FeatureExtractor(padding=0)` matches pipecat's vendored extractor exactly (max diff 0.0), so no code was copied. **Preemptive LLM start (approved as b): not built** — with Smart Turn the pipeline already starts ~0.03 s after the 0.2 s pause on complete turns, so preemption could save ≤ 0.3 s only on "incomplete" turns, which it would mostly waste | Model card lists Hindi among 23 languages. Owner's 20 Phase 0 recordings (0.2 s trailing silence): complete → complete, English 15/15 and Hinglish 4/5 (miss: "Mummy ko message bhejo…", p = 0.05, falls back to 0.5 s); cut at 55% → incomplete, Hinglish 5/5 but English 4/15 (a crude cut often ends grammatically; falling back loses nothing). 31 ms median per check. Replay (`tests/evals/wake_junk_replay.py`): command endpoint 513 ms → 266–309 ms, all 4 scenarios pass. **Also:** "soya" removed from the D54 veto list — turbo wrote a real synthetic "Hey Zoya" as "Hey, Soya." and wrote the owner's real one as "Soya!"; owner clips still 12/13 wakes, 0/4 false | §9.1, §13.5.2 |
| D56 | 2026-09-14 | **Echo cancellation for laptop speakers: owner chose option (2).** Phase 2 hands off with the D52/D53 speaker limits: headset for hands-free with music or barge-in, and fn + Shift. A separate session builds WebRTC AEC3 (`livekit`) with the reference = our TTS PCM + a Core Audio process tap of other apps (`pyobjc-framework-CoreAudio`), in parallel; it owns `zoya/voice.py` and `zoya/audio.py` next | Owner's live run: music stops "Hey Zoya" (wakes 4/5 → 1/5 as music rises from 10% to equal voice RMS); "stop" unheard during TTS. Apple voice processing doesn't cancel other apps. Research, measurements, design, deps and risks: AUDIT.md §5 | AUDIT §5, D52, D53 |
| D57 | 2026-09-14 | **Final-run fixes (coordinator triage of the owner's run; evidence `logs/timing.log` 10:23–10:55 UTC):** (1) after a wake, wait 6 s for the command with other audio ducked the whole time; junk in the window no longer restores the volume. (2) turbo transcripts are accepted only in en/hi, and the brain answers in English or romanized Hinglish. (3) runaway repeats (compression ratio > 2.4, ≥ 4 identical words in a row, > 60 words) are junk. (4) junk no longer reaches the brain: mean avg_logprob < −0.7 (owner's 20 real commands −0.07…−0.44; chime/music −0.74…−1.06), one-word fragments unless a rule knows them, captured segments < 20% user voice, and a repeated "Hey Zoya" inside the window (turbo: "He's real." / "He is aware.") is a re-wake, not a command; no follow-up window after "play …". (5) polite prefixes stripped before the rules (now / hey / ok / just / also / "I want you to"), "stop playing the song" → pause. (6) "open X in a/the browser / in Chrome / on the web" → orchestrator, never open_app. (7) prompt: media, opening, searching and reading never need confirmation, only pay/send/delete/submit/post; media_control can't pick a song, so Zoya says what she actually did (a live check caught "Playing Love Me Not on Spotify." as a false claim) | Replays in `tests/evals/wake_junk_replay.py`, 6/6: chime, music, radio-under-command 0.9 s, follow-up, wait-over-music (volume 60 → 18 → 60 once), repeated-wake. Vietnamese TTS "Chế gió là…" → dropped; owner Hinglish kept. Brain live check: "play love me not" → "I can only control music already loaded…". **Known, AEC session:** background radio speech can still be dispatched in a follow-up window once the volume is restored | D53, D56 |
